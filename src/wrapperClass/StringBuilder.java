package wrapperClass;

public class StringBuilder {
    public static void main(String[] args) {
        /*
        * StringBuilder
        * 1)一个可变的字符序列。此类提供一个与 StringBuffer 兼容的API，但不保证同步（不是线程安全）
        * 2）主要操作时 append 和 insert方法、重载
        * */
    }
}
